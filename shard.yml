name: replay_fetcher
version: 0.1.0

# authors:
#   - name <email@example.com>

# description: |
#   Short description of replay_fetcher

dependencies:
  aws-credentials:
    github: y2k2mt/aws-credentials
    version: 0.3.0
  aws_signer:
    github: beanieboi/aws-signer.cr
# development_dependencies:
#   webmock:
#     github: manastech/webmock.cr

# license: MIT
